import { RemoverMascarasDirective } from './remover-mascaras.directive';

describe('RemoverMascarasDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoverMascarasDirective();
    expect(directive).toBeTruthy();
  });
});
